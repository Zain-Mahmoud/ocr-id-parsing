# ocr-id-parsing

Instructions:
- To generate the fine-tuning synthetic data, run `./gen.sh sample_size augmentation_batch_size` where `sample_size` is the number of unaugmented samples to generate and `augmentation_batch_size` is the number of augments to generate for each image.
